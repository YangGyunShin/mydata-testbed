spring:
  application:
    name: mydata-testbed

  # Thymeleaf ??
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
    encoding: UTF-8

  # ?????? ?? (H2)
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  # JPA ??
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

  # H2 ??
  h2:
    console:
      enabled: true
      path: /h2-console

  # ?? ??
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# ?? ??
server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      force: true

# ?? ??? ??
file:
  upload-dir: ./uploads
  max-size: 10MB

# ?? ??
logging:
  level:
    root: INFO
    com.mydata.testbed: DEBUG
    org.hibernate.SQL: DEBUG